package game.objects.bullets
{
	import game.objects.OBJECT_TYPE;

	public class Bullet extends BulletBase
	{
		public function Bullet(_x:Number, _y:Number, _type:OBJECT_TYPE)
		{
			super(_x, _y, _type);
		}
		
	}
}